# CSS Delete Candidates

These files were moved here instead of being permanently deleted.

## Moved Files

- `chatbot.css`: no active in-repository stylesheet reference was found.
- `gallery_top.css`: no active in-repository stylesheet reference was found.
- `サンプルline_animation.css`: no active in-repository stylesheet reference was found.
- `font.css`: no active root-page or legacy `src/` template link or active duration-variable consumer remains.
- `matchingミス.css`: no active Webpack import or page reference was found.

## Safety Boundary

- The files were backed up at `/tmp/website-css-delete-backup-20260907/` before moving.
- The files are retained here for review and rollback; they were not permanently deleted.
- `docs/css/` was not moved because it is generated output and is rebuilt by `webpack.config.js`.
- Legacy CSS referenced by `_layoutsdefault.html` remains in the source `css/` directory.

## Restore

Restore only an explicitly approved file after a fresh reference scan and visual verification.
